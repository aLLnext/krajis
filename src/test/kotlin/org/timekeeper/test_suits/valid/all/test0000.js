var tips = [
    "Click on any AST node with a '+' to expand it",

    "Shift click on an AST node expands the whole substree"
];

function printTips() {
    tips.forEach((tip, i) => console.log(`Tip ${i}:` + tip));
}

var a;
a = 'Hello';

var message = 2;
var t = 2
message = 'Hello';

var v = 2, с, b = -2;

var t22 = a + b;

var v1, b1 = -2;

(function test22(t, tt) {
})

function foo(a, b) {
    function f() {
    }

    function g() {
        return
    }

    {
    }
    return function (x) {
        return a * x + b
    }
}

var bar = foo(2, 3)(5)
var c = function () {
}
c()

if (bar < 10 && false, foo === null, true) {
    while ("What a nice place!", bar !== 0 || 2 + 2 === 4) {
        if (bar <= 0)
            continue
        else {
            bar = bar + 1
            break;
        }
    }
}

var a = [, x,]
a = []
a = [, , x, , y, ,]

var o = {}
o = {x: 42, while: "true", "x =": 4, 2: "is the answer",}
o[2] = o.x + o['while']
delete x['true']
delete o.x

var x = 3, y, z = 5 + "In my heart I'm number."
y = x + (z - y) * 5 / 2 % z >>> 2e7 >> x << y | x & y ^ y
y = !y + ~y + +y + -y

if (y < x) ;
if (y > x) ;
if (y <= x) ;
if (y >= x) ;
if (y === x) ;
if (y !== x) ;
{
    foo();
}

x + y + z

var greeting = {
    fullname: "Michael Jackson",
    greet: (message, name) => {
        console.log(message + name + "!!");
        print("test");
    }
};

~x + y

do {

} while (true)


const a1 = 1;


function hello() {
    sayHi(x + y);
}